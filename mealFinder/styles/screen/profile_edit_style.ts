import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    margin: 8,
  },
  textContainer: {
    marginBottom: 20,
  },
  emailContainer: {
    marginBottom: 20,
  },
  emailLabel: {
    marginBottom: 10,
  },
  passwordContainer: {
    marginBottom: 20,
  },
  passwordLabel: {
    marginBottom: 10,
  },
  input: {
    height: 40,
    borderWidth: 1,
    padding: 10,
  },
});
